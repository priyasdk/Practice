//Merge sort
#include<stdio.h>

int mergesort(int a[],int start, int end)
{
  while(start<end)
  {
    mid=(start+end)/2;
    mergesort(a,0,mid);
    mergesort(a,mid+1,n);
  }
}

int merge(int a[],int start,int end)
{
  int i,j;
  n1=
}
